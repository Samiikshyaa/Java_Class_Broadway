package Array;

public class ArrayDoc {

	public static void main(String[] args) {
	//when to use how to use read write
		//collection of homogeneous data
		//collection or group of multiple values of same data type
		//array has fixed size
		// data stored on the basis of indices(index starts from 0)
		//random access
		//contains duplicate data
		//contiguous memory allocation (attached vayera aaucha) a[5]=======a[][][][][]
		//array itself is an object in Java
		
		//syntax:  data type  array name[] = new datatype_size;
		//example:
		//float h[]=new float[5000];
		//int fee[]=new fee[200];
		//String cname[]= new  country[150];
		//char[] vs = new char[5];
		//Employee emps[]=new Employee[3000];
}
}
